const userConfig = {
  role: {
    admin: {
      slug: 'admin',
      enrollmentLength: 7,
    },
    student: {
      slug: 'student',
      enrollmentLength: 14,
    },
  },
};

export default userConfig;
