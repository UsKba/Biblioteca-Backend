# Migration `20210110133513-relations-renamed`

This migration has been generated by DestroyeerU at 1/10/2021, 1:35:13 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20210109235922-create-messages-and-messages-tags..20210110133513-relations-renamed
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 model User {
   id         Int    @id @default(autoincrement())
@@ -21,13 +21,13 @@
   userReserve UserReserve[]
   reserve     Reserve[]
-  friendUser1 Friend[] @relation("user1")
-  friendUser2 Friend[] @relation("user2")
+  friendUser1 Friend[] @relation("friendUser1")
+  friendUser2 Friend[] @relation("friendUser2")
-  friendRequestSender   FriendRequest[] @relation("sender")
-  friendRequestReceiver FriendRequest[] @relation("receiver")
+  friendRequestSender   FriendRequest[] @relation("friendRequestUserSender")
+  friendRequestReceiver FriendRequest[] @relation("friendRequestUserReceiver")
   messageUserSender   Message[] @relation("messageUserSender")
   messageUserReceiver Message[] @relation("messageUserReceiver")
 }
@@ -41,10 +41,10 @@
 model Friend {
   id Int @id @default(autoincrement())
-  user1 User @relation("user1", fields: [userId1], references: [id])
-  user2 User @relation("user2", fields: [userId2], references: [id])
+  user1 User @relation("friendUser1", fields: [userId1], references: [id])
+  user2 User @relation("friendUser2", fields: [userId2], references: [id])
   userId1 Int
   userId2 Int
 }
@@ -52,10 +52,10 @@
 model FriendRequest {
   id     Int @id @default(autoincrement())
   status Int
-  userSender   User @relation("sender", fields: [senderId], references: [id])
-  userReceiver User @relation("receiver", fields: [receiverId], references: [id])
+  userSender   User @relation("friendRequestUserSender", fields: [senderId], references: [id])
+  userReceiver User @relation("friendRequestUserReceiver", fields: [receiverId], references: [id])
   senderId   Int
   receiverId Int
 }
```


