# Migration `20210327165237-create-notice-room`

This migration has been generated by DestroyeerU at 3/27/2021, 4:52:38 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."User" (
"colorId" INTEGER NOT NULL  ,"email" TEXT NOT NULL  ,"enrollment" TEXT NOT NULL  ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"name" TEXT NOT NULL  ,"roleId" INTEGER NOT NULL  ,FOREIGN KEY ("roleId") REFERENCES "Role"("id") ON DELETE CASCADE ON UPDATE CASCADE,
FOREIGN KEY ("colorId") REFERENCES "Color"("id") ON DELETE CASCADE ON UPDATE CASCADE)

CREATE TABLE "quaint"."Role" (
"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"slug" TEXT NOT NULL  )

CREATE TABLE "quaint"."Friend" (
"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"userId1" INTEGER NOT NULL  ,"userId2" INTEGER NOT NULL  ,FOREIGN KEY ("userId1") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE,
FOREIGN KEY ("userId2") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE)

CREATE TABLE "quaint"."FriendRequest" (
"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"receiverId" INTEGER NOT NULL  ,"senderId" INTEGER NOT NULL  ,"status" INTEGER NOT NULL  ,FOREIGN KEY ("senderId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE,
FOREIGN KEY ("receiverId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE)

CREATE TABLE "quaint"."Reserve" (
"adminId" INTEGER NOT NULL  ,"date" DATE NOT NULL  ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"name" TEXT   ,"roomId" INTEGER NOT NULL  ,"scheduleId" INTEGER NOT NULL  ,FOREIGN KEY ("roomId") REFERENCES "Room"("id") ON DELETE CASCADE ON UPDATE CASCADE,
FOREIGN KEY ("scheduleId") REFERENCES "Schedule"("id") ON DELETE CASCADE ON UPDATE CASCADE,
FOREIGN KEY ("adminId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE)

CREATE TABLE "quaint"."UserReserve" (
"colorId" INTEGER NOT NULL  ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"reserveId" INTEGER NOT NULL  ,"status" INTEGER NOT NULL  ,"userId" INTEGER NOT NULL  ,FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE,
FOREIGN KEY ("colorId") REFERENCES "Color"("id") ON DELETE CASCADE ON UPDATE CASCADE,
FOREIGN KEY ("reserveId") REFERENCES "Reserve"("id") ON DELETE CASCADE ON UPDATE CASCADE)

CREATE TABLE "quaint"."Color" (
"color" TEXT NOT NULL  ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT)

CREATE TABLE "quaint"."Room" (
"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"initials" TEXT NOT NULL  ,"status" INTEGER NOT NULL  )

CREATE TABLE "quaint"."Schedule" (
"endHour" TEXT NOT NULL  ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"initialHour" TEXT NOT NULL  ,"periodId" INTEGER NOT NULL  ,FOREIGN KEY ("periodId") REFERENCES "Period"("id") ON DELETE CASCADE ON UPDATE CASCADE)

CREATE TABLE "quaint"."Period" (
"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"name" TEXT NOT NULL  )

CREATE TABLE "quaint"."Tag" (
"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"name" TEXT NOT NULL  )

CREATE TABLE "quaint"."Message" (
"content" TEXT NOT NULL  ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"receiverId" INTEGER NOT NULL  ,"senderId" INTEGER NOT NULL  ,"subject" TEXT NOT NULL  ,FOREIGN KEY ("senderId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE,
FOREIGN KEY ("receiverId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE)

CREATE TABLE "quaint"."MessageTags" (
"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"messageId" INTEGER NOT NULL  ,"tagId" INTEGER NOT NULL  ,FOREIGN KEY ("tagId") REFERENCES "Tag"("id") ON DELETE CASCADE ON UPDATE CASCADE,
FOREIGN KEY ("messageId") REFERENCES "Message"("id") ON DELETE CASCADE ON UPDATE CASCADE)

CREATE TABLE "quaint"."ComputerLocal" (
"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"name" TEXT NOT NULL  )

CREATE TABLE "quaint"."Computer" (
"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"identification" TEXT NOT NULL  ,"localId" INTEGER NOT NULL  ,"status" INTEGER NOT NULL  ,FOREIGN KEY ("localId") REFERENCES "ComputerLocal"("id") ON DELETE CASCADE ON UPDATE CASCADE)

CREATE TABLE "quaint"."Notice" (
"content" TEXT NOT NULL  ,"createdAt" DATE NOT NULL DEFAULT CURRENT_TIMESTAMP ,"expiredAt" DATE NOT NULL  ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"imageCode" INTEGER NOT NULL  ,"title" TEXT NOT NULL  ,"userCreatorId" INTEGER NOT NULL  ,FOREIGN KEY ("userCreatorId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE)

CREATE TABLE "quaint"."NoticeRoom" (
"noticeId" INTEGER NOT NULL  ,"roomId" INTEGER NOT NULL  ,"status" INTEGER NOT NULL  ,
    PRIMARY KEY ("noticeId"),FOREIGN KEY ("noticeId") REFERENCES "Notice"("id") ON DELETE CASCADE ON UPDATE CASCADE,
FOREIGN KEY ("roomId") REFERENCES "Room"("id") ON DELETE CASCADE ON UPDATE CASCADE)

CREATE UNIQUE INDEX "quaint"."User.enrollment" ON "User"("enrollment")

CREATE UNIQUE INDEX "quaint"."User.email" ON "User"("email")

CREATE UNIQUE INDEX "quaint"."Role.slug" ON "Role"("slug")

CREATE UNIQUE INDEX "quaint"."Room.initials" ON "Room"("initials")

CREATE UNIQUE INDEX "quaint"."Period.name" ON "Period"("name")

CREATE UNIQUE INDEX "quaint"."Tag.name" ON "Tag"("name")

CREATE UNIQUE INDEX "quaint"."ComputerLocal.name" ON "ComputerLocal"("name")

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20210327162050-add-image-code-on-notice..20210327165237-create-notice-room
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 model User {
   id         Int    @id @default(autoincrement())
@@ -103,9 +103,10 @@
   id       Int    @id @default(autoincrement())
   initials String @unique
   status   Int
-  reserve Reserve[]
+  reserve    Reserve[]
+  NoticeRoom NoticeRoom[]
 }
 model Schedule {
   id          Int    @id @default(autoincrement())
@@ -182,7 +183,20 @@
   createdAt DateTime @default(now())
   expiredAt DateTime
-  userCreator   User @relation(fields: [userCreatorId], references: [id])
+  userCreator   User         @relation(fields: [userCreatorId], references: [id])
   userCreatorId Int
+  NoticeRoom    NoticeRoom[]
+}
+
+model NoticeRoom {
+  notice   Notice @relation(fields: [noticeId], references: [id])
+  noticeId Int
+
+  room   Room @relation(fields: [roomId], references: [id])
+  roomId Int
+
+  status Int
+
+  @@id([noticeId])
 }
```


