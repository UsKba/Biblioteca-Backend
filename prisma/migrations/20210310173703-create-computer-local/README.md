# Migration `20210310173703-create-computer-local`

This migration has been generated by DestroyeerU at 3/10/2021, 5:37:03 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."ComputerLocal" (
"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"name" TEXT NOT NULL  )

CREATE TABLE "quaint"."new_Computer" (
"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"identification" TEXT NOT NULL  ,"localId" INTEGER NOT NULL  ,"status" INTEGER NOT NULL  ,FOREIGN KEY ("localId") REFERENCES "ComputerLocal"("id") ON DELETE CASCADE ON UPDATE CASCADE)

INSERT INTO "quaint"."new_Computer" ("id", "identification", "status") SELECT "id", "identification", "status" FROM "quaint"."Computer"

PRAGMA foreign_keys=off;
DROP TABLE "quaint"."Computer";;
PRAGMA foreign_keys=on

ALTER TABLE "quaint"."new_Computer" RENAME TO "Computer";

CREATE UNIQUE INDEX "quaint"."Computer.identification" ON "Computer"("identification")

CREATE UNIQUE INDEX "quaint"."ComputerLocal.name" ON "ComputerLocal"("name")

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20210213180500-computers-renamed-to-singular..20210310173703-create-computer-local
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 model User {
   id         Int    @id @default(autoincrement())
@@ -155,14 +155,23 @@
   tagId     Int
   messageId Int
 }
+model ComputerLocal {
+  id   Int    @id @default(autoincrement())
+  name String @unique
+
+  Computers Computer[]
+}
+
 model Computer {
   id Int @id @default(autoincrement())
   identification String @unique
-  local          String
   status         Int
+
+  local   ComputerLocal @relation(fields: [localId], references: [id])
+  localId Int
 }
 model Notice {
   id Int @id @default(autoincrement())
```


