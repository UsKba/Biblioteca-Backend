generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("PRISMA_URL")
}

model User {
  id         Int @default(autoincrement()) @id
  enrollment String @unique
  password   String
  name       String
  email      String
}
